### Issues related to get/calendar
1) It gives an error sometimes by saying that it can't process the query, while sometimes it gives the count.
2) On checking the dashboard: i can see 20 events occuring today, however when i ran the query on postman with the start and end date as today, it shows some other number which is in hundreds. On the chatgpt prompt, it keeps switching between 5 and 1907, both of these don't match the number on postman.

### Issue with get/calendar/coverage and get/calendar/{eventiD}
This works perfectly fine in this repo. However when run on the chatGPT model that was configured earlier, it gives an error and does not process.
The output received from these endpoints matches the postman output too!
